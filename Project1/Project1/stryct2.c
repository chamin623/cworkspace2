#include <stdio.h>

/*
void swap(int x, int y) {
	int z = x;
	x = y;
	y = z;
	printf("%d, %d\n", x, y);
}

int main(void) {
	int x = 5, y = 10, z;

	printf("두 수의 교환\n");
	swap(x, y); 

void swap(int xy[]) {
	int z = xy[0];
	xy[0] = xy[1];
	xy[1] = z;
}

int main(void) {
	int xy[] = { 5, 10 }, z;

	printf("두 수의 교환\n");
	swap(xy);
	printf("%d, %d\n", xy[0], xy[1]);


	void swap(int* x, int * y) {
		int z = *x;
		*x = *y;
		*y = z; 
	}

	int main(void) {
		int x = 5, y = 10, z;

		printf("두 수의 교환\n");
		swap(&x, &y);
		printf("%d, %d\n", x, y);*/

/*
typedef struct XY {
	int x, y, z;
}xy;

void swap(xy* ab) {
	ab->z = ab->x;
	ab->x = ab->y;
	ab->y = ab->z;
}

int main(void) {
	xy sw = { 5, 10, 0 };

	printf("두 수의 교환\n");
	swap(&sw);
	printf("%d, %d\n", sw.x, sw.y);
	return 0;
}
*/
